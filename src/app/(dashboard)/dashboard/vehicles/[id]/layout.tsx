export const metadata = {
  title: 'Vehicle Details | Recondition House',
  description: 'Vehicle Details',
};

const ProfileDetailsLayout = ({ children }: any) => {
  return <>{children}</>;
};

export default ProfileDetailsLayout;
