export const metadata = {
  title: 'Vehicle Add | Recondition Hub',
  description: 'Vehicle Details',
};

const AddVehicleLayout = ({ children }: any) => {
  return <>{children}</>;
};

export default AddVehicleLayout;
